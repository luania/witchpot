<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/tools">

    <data>

        <import type="com.luania.witchpot.util.MediaUtil"/>
        <variable
            name="segmentPojo"
            type="com.luania.witchpot.pojo.SegmentPojo"/>
    </data>

    <android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:fresco="http://schemas.android.com/apk/res-auto"
        android:id="@+id/cardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/half_margin"
        android:foreground="?attr/selectableItemBackground"
        android:clickable="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <com.facebook.drawee.view.SimpleDraweeView
                android:id="@+id/simpleDraweeView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                fresco:viewAspectRatio="1"
                app:imageURI="@{MediaUtil.getLittleImage(segmentPojo.decodeImage())}"
                android:background="@color/colorBackground" />

            <TextView
                android:id="@+id/tvTitle"
                android:text="@{segmentPojo.decodeTitle()}"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/base_margin" />
        </LinearLayout>
    </android.support.v7.widget.CardView>
</layout>